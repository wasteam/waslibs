<UserControl
    x:Class="AppStudio.Uwp.Samples.SectionListHelp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppStudio.Uwp.Samples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackground}">
        <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
            <RichTextBlock Margin="24" TextWrapping="Wrap" Foreground="{StaticResource AppForeground}">
                <Paragraph>
                    <Run FontSize="32" Text="SectionList Help" />
                </Paragraph>
                <Paragraph/>
                <Paragraph>
                    <Run Text="SectionList es  un control que facilita la visualización de elementos en disposición horizontal. Cada SectionListItem puede albergar cualquier código XAML en su interior, y además dispone de unas propiedades que le permiten tener un Header, un ViewAll Button además de poder gestionar la visualización de un progress bar de carga y un mensaje de error de carga."/>
                </Paragraph>
            </RichTextBlock>
        </ScrollViewer>
    </Grid>
</UserControl>
