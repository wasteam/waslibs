<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AppStudio.Uwp.Controls">

    <Style TargetType="controls:Virtualbox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:Virtualbox">
                    <Canvas Background="{TemplateBinding Background}">
                        <ContentPresenter Name="content" Width="{TemplateBinding VirtualWidth}" Height="{TemplateBinding VirtualHeight}">
                            <ContentPresenter.RenderTransform>
                                <CompositeTransform />
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
